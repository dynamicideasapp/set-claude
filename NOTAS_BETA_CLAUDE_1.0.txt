=========================================================
NOTAS BETA CLAUDE 1.0 – SET FOTOGRÁFICO
Creada por Hernán Díaz · Dynamic Ideas
=========================================================

VERSIÓN ACTUAL: APP BETA CLAUDE 1.3 – BUILD 2026-04-21
APK: SET_FOTOGRAFICO_v1.3_beta.apk
ID App: com.dynamicideas.setfotografico

=========================================================
SESIÓN CLAUDE – 2026-04-21 (DÉCIMA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS — APK v1.3_beta:

1. FIRMA DIGITAL – REDISEÑO COMPLETO DEL PDF
   - Imagen de firma ahora ocupa el canvas completo (sin recorte)
     El trazo puede cruzar la línea guía hacia abajo como firma real
   - Tamaño aumentado de 50mm a 110mm de ancho en el PDF
   - La imagen se posiciona centrada sobre la línea de firma:
     el 60% del alto de la imagen queda alineado con la línea horizontal
     del PDF, logrando el efecto de firma sobre la línea
   - La línea horizontal se dibuja encima de la imagen (superpuesta)
     al ancho exacto del texto del nombre (usando doc.getTextWidth)
   - Separación entre línea y nombre aumentada de 3mm a 10mm (1cm)
   - Archivos: www/app.js → confirmarFirmaPad() sin recorte de canvas
               www/pdf-logic.js → SIG_MAX_W 110mm, posición sigLineY al 60%

2. FIRMA DIGITAL – ORIENTACIÓN LANDSCAPE EN APK
   - Al abrir el pad de firma, se bloquea la orientación en landscape
     via screen.orientation.lock('landscape') con manejo async correcto
   - El canvas se inicializa DESPUÉS de que la orientación se establece
     (setTimeout 120ms tras el lock) para usar las dimensiones reales
   - Al confirmar o cancelar, se hace unlock() para liberar la orientación
   - En navegador de escritorio: el lock falla silenciosamente y el modal
     se muestra en portrait sin errores
   - Archivos: www/app.js → abrirPadFirma(), _firmaInitCanvas(),
               _cerrarFirmaPadOrientacion()

3. VS CODE LIVE SERVER – CONFIGURACIÓN CHROME
   - CustomBrowser: "chrome" para lanzar Chrome directo sin pasar por xdg-open
   - file: "/index.html" fija el archivo de entrada y evita doble pestaña
   - Brave vuelve como predeterminado del sistema (xdg-settings) para evitar
     conflicto que causaba demora excesiva al abrir Chrome
   - Archivos: .vscode/settings.json

4. INDEX.HTML RAÍZ RESTAURADO Y COMMITEADO
   - El archivo index.html de la raíz fue restaurado del historial git
     (había sido eliminado en un commit anterior de "eliminación de duplicados")
   - Es necesario para que Live Server sirva la app correctamente desde VS Code
   - Commiteado por separado con mensaje explicativo

APK GENERADA:
   - Archivo: SET_FOTOGRAFICO_v1.3_beta.apk
   - Build: 2026-04-21 · Gradle assembleDebug exitoso
   - APKs anteriores debug (v3.8–v4.6) eliminadas del repositorio
   - Esquema de versiones renombrado a: v1.0_beta, v1.1_beta, v1.2_beta, v1.3_beta

=========================================================
SESIÓN CLAUDE – 2026-04-21 (NOVENA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS:

1. GITHUB ACTION – DEPLOY AUTOMÁTICO A GITHUB PAGES
   - Agregado .github/workflows/deploy.yml
   - Cada push a main despliega automáticamente la carpeta www/ a GitHub Pages
   - Eliminados los archivos duplicados de la raíz (app.js, index.html, style.css,
     pdf-logic.js, sw.js, manifest.json, libs/) — la fuente única queda en www/
   - Archivos: .github/workflows/deploy.yml

2. FIXES GITHUB PAGES
   - Agregado heic2any.min.js en libs/ raíz para compatibilidad con Pages
   - Agregado sw.js en raíz y corregido meta mobile-web-app-capable
   - Archivos: www/index.html, sw.js

APK GENERADA:
   - Archivo: SET_FOTOGRAFICO_v1.2_beta.apk

=========================================================
SESIÓN CLAUDE – 2026-04-21 (OCTAVA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS:

1. PAD DE FIRMA DIGITAL – TRAZADO A MANO EN PANTALLA
   - Reemplaza la opción de subir imagen de firma por un canvas táctil
   - El usuario dibuja su firma directamente en pantalla con el dedo
   - Nombre y cargo se muestran debajo del trazo en el canvas (no solo en PDF)
   - Landscape forzado durante el trazado via CSS + Screen Orientation API
     para dar más espacio horizontal al firmar
   - La firma generada se guarda en IndexedDB igual que antes
   - Archivos: www/app.js → pad canvas, www/index.html → modal pad,
               www/style.css → estilos landscape/canvas

2. SISTEMA DE TOAST – REEMPLAZA alert() POR NOTIFICACIONES VISUALES
   - Todos los alert() del sistema reemplazados por toasts no bloqueantes
   - El toast aparece en la parte superior de la pantalla y desaparece solo
   - No interrumpe el flujo del usuario (alert() bloqueaba la UI en Android)
   - Archivos: www/app.js → función showToast(), www/index.html → #toast-container,
               www/style.css → estilos .toast

3. INTEGRACIÓN SENTRY – MONITOREO AUTOMÁTICO DE ERRORES
   - Sentry SDK cargado via loader script con DSN del proyecto
   - Captura automática de errores JS no controlados en producción
   - Configurado correctamente: sentryOnLoad definido ANTES del loader script
   - Eliminado tracesSampleRate incompatible con el loader bundle
   - Archivos: www/index.html

=========================================================
SESIÓN CLAUDE – 2026-04-20 (SÉPTIMA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS:

1. FIX COMPARTIR PDF
   - Antes: chequeo isIOS no activaba diálogo nativo en Chrome Android
   - Ahora: usa navigator.canShare() para detectar soporte real del navegador
   - Activa diálogo nativo en Chrome Android, Safari iOS y Safari macOS
   - Fallback a window.open() para browsers sin soporte
   - Fix adicional: el fallback usa URL.createObjectURL() en vez de data URL
     (Chrome bloquea window.open() con data URLs por seguridad)
   - Archivos: www/app.js

2. FIX BOTONES CONTACTO
   - Problema: window.open() bloqueado por los popups blocker en Android
   - Solución: reemplazado por creación dinámica de elemento <a> con click()
   - Los botones de teléfono y dirección ahora abren sin bloqueo
   - Archivos: www/app.js

3. ELIMINA BOTÓN Y CÓDIGO "VERSIÓN WEB / ESCRITORIO"
   - Removido botón #btnModoEscritorio del HTML
   - Eliminadas funciones initModoEscritorio() y toggleModoEscritorio() del JS
   - Eliminadas reglas CSS body.modo-escritorio
   - Archivos: www/app.js, www/index.html, www/style.css

4. FIX IDs FALTANTES EN FORMULARIO
   - Agregados IDs faltantes: sheetSizeNote, fontNote, noteColorNote
   - Eliminado código muerto: función selectReportMembreteMode
   - Archivos: www/app.js, www/index.html

=========================================================
SESIÓN CLAUDE – 2026-04-20 (SEXTA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS:

1. MIGRACIÓN DE ALMACENAMIENTO A INDEXEDDB (SOLUCIÓN LÍMITE 5MB)
   - Problema: imágenes de membrete, firmas y PDFs saturaban el localStorage (5MB)
   - Las fotos ya usaban IndexedDB; ahora también las demás imágenes y PDFs
   - Se agregó store "kvstore" a la base IndexedDB existente (DB_VERSION 1 → 2)
   - Nuevas funciones: idbKvGet(), idbKvSet() como helpers genéricos clave-valor
   - Cache en memoria (_imgCache) para mantener getters síncronos sin cambiar la UI
   - Migración automática al arrancar: lee datos viejos de localStorage,
     los mueve a IndexedDB y los elimina de localStorage (sin perder datos)
   - Claves migradas a IndexedDB:
     · set_report_membrete_image_v1 (imagen membrete del informe)
     · set_report_sig_image_v1 (firma digital del informe)
     · set_new_template_sig_image_v1 (firma digital de plantilla)
     · set_new_template_membrete_image_v1 (imagen membrete de plantilla)
     · set_pdf_library_v1 (biblioteca de PDFs generados)
   - Eliminado el código de fallback por cuota de localStorage en savePdfLibrary
   - Configuraciones pequeñas (tema, fuente, colores, etc.) siguen en localStorage
   - Archivos: www/app.js, app.js

2. ÍCONO FLECHA ATRÁS EN BOTONES "VOLVER ATRÁS"
   - Todos los botones "VOLVER ATRÁS" ahora muestran un chevron SVG minimalista
   - El ícono usa currentColor (hereda el blanco del texto automáticamente)
   - CSS: .btn-sec ahora tiene display:flex + align-items:center + gap:8px
   - Archivos: www/index.html, index.html, www/style.css, style.css

3. MEJORA TRACKING GOOGLE ANALYTICS EN MÓVIL Y APK
   - APK (Capacitor): cuando hostname === 'localhost', fuerza page_location
     a https://app.setfotografico.cl/ para que GA4 unifique las visitas
   - Móvil (navegador): agregado transport_type:'beacon' para enviar el hit
     incluso si el usuario cierra la página antes de que cargue el script async
   - Eliminado noscript GTM huérfano (sin script principal) del index.html raíz
   - Archivos: www/index.html, index.html

=========================================================
SESIÓN CLAUDE – 2026-04-19 (QUINTA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS — APK v4.3:

1. BOTÓN TOMAR FOTOGRAFÍA SEPARADO DE GALERÍA
   - Antes: un solo botón "📷 TOMAR / GALERÍA" que solo abría la galería
   - Ahora: dos botones separados, uno bajo el otro, mismo diseño azul:
     · "📷 TOMAR FOTOGRAFÍA" → abre cámara nativa directamente
       (usa capture="environment" en el input file)
     · "🖼️ GALERÍA" → abre la galería del dispositivo (comportamiento anterior)
   - Las fotos tomadas desde cámara quedan SOLO en la memoria de la app,
     NO se guardan en la galería del teléfono (decisión deliberada)
   - Archivos: www/index.html → sección s3 (pantalla de fotografías)
             www/app.js → previsualizar() acepta inputEl como parámetro
             www/style.css → clase .btn-galeria agregada

PENDIENTE PARA PRÓXIMA SESIÓN:
   - Nota de voz por foto: el usuario toma la foto y puede grabar una nota
     de voz debajo de ella como recordatorio de descripción en terreno
   - Dos variantes a evaluar:
     · Solo recordatorio (graba/reproduce, el usuario escribe después)
     · Transcripción automática con SpeechRecognition API (sin costo,
       nativa en Android, convierte voz a texto directo en el textarea)
   - Preferencia del usuario: evaluar mañana

=========================================================
SESIÓN CLAUDE – 2026-04-19 (CUARTA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS (8 EN TOTAL) — APK v4.2:

1. VELOCIDAD DE CARGA DE FOTOS (MEJORA #1)
   - Antes: la miniatura aparecía recién DESPUÉS de comprimir (3-4 seg de espera)
   - Ahora: la foto se muestra al instante con URL.createObjectURL() mientras
     la compresión corre en segundo plano y reemplaza silenciosamente la imagen
   - Además: resizeQuality cambiado de "high" a "medium" para acelerar compresión
   - Archivos: www/app.js → función previsualizar()

2. FECHA PREDETERMINADA CON TIMEZONE CHILE (MEJORA #2)
   - Antes: new Date().toISOString().split('T')[0] usaba UTC → después de las
     21:00/20:00 chilenas mostraba el día siguiente como fecha por defecto
   - Ahora: fecha calculada con getFullYear/getMonth/getDate (hora local del
     dispositivo), sin conversión UTC
   - Archivos: www/app.js → líneas donde se inicializa la fecha (2 ocurrencias)

3. TÍTULO DEL SET MÁS SEPARADO DEL MEMBRETE EN PDF (MEJORA #3)
   - Antes: 6mm de separación fija entre membrete y título del set
   - Ahora: 6mm + 3 líneas extra (~9.3mm adicionales) de separación visual
   - Archivos: www/pdf-logic.js → cálculo de posición del título

4. TERCERA LÍNEA EN EL FOOTER DE LA APP (MEJORA #4)
   - Antes: footer con 2 líneas ("CREADA POR HERNAN DIAZ DINAMARCA" + versión)
   - Ahora: 3 líneas, la tercera dice "FUNCIONANDO HASTA EL 20.05.2026"
   - Archivos: www/app.js → función setFooter() (textContent → innerHTML)

5. FECHA DE VENCIMIENTO DE LA APK AL 20/05/2026 (MEJORA #5)
   - La app verifica al iniciar si la fecha local supera el 20/05/2026 00:00
   - Si venció: oculta toda la app y muestra pantalla con logo y mensaje
     "SU VERSIÓN DE PRUEBA HA TERMINADO / Contacte al administrador"
   - Usa fecha LOCAL del dispositivo (no UTC), consistente con Mejora #2
   - Verificación ocurre después del splash y antes de cargar la UI principal
   - Archivos: www/app.js → constante EXPIRY_DATE + función checkExpiry()

6. BOTÓN "VOLVER ATRÁS" EN PANTALLA DE REVISIÓN FINAL (MEJORA #6)
   - Agregado botón "← Volver atrás" entre "+ Agregar más fotos" y
     "🗑️ BORRAR E IR A INICIO" en la pantalla de revisión (paso 5)
   - Permite retroceder al paso 4 sin perder los datos del set
   - Archivos: www/index.html → sección paso 5

7. MODAL DE CONFIRMACIÓN AL ELIMINAR FOTO (MEJORA #7)
   - Antes: confirm() nativo del sistema (pequeño, fácil de aceptar por error)
   - Ahora: modal personalizado a pantalla completa con:
     · Botón "ELIMINAR" (rojo, requiere tap deliberado)
     · Botón "CANCELAR" (prominente, seguro)
   - Archivos: www/index.html → modal #deletePhotoModal
             www/app.js → funciones eliminarFoto(), cerrarModalEliminar(),
                          confirmarEliminarFoto(), variable _pendingDeleteIndex

8. BOTÓN "VERSIÓN WEB" CON DISEÑO APPLE SOLO EN NAVEGADOR (MEJORA #8)
   - Solo visible cuando la app corre en navegador (no en APK Android)
   - Detectado con window.Capacitor?.isNativePlatform?.()
   - Al presionar activa clase CSS "modo-escritorio" en el body
   - Diseño tipo Apple activado: fondo #f5f5f7, tipografía SF Pro,
     max-width 820px centrado, bordes redondeados, sombras suaves,
     fotos más grandes, transiciones suaves
   - Botón alterna entre "🖥️ CAMBIAR A VERSIÓN WEB" y "📱 CAMBIAR A VERSIÓN MÓVIL"
   - Archivos: www/app.js → funciones initModoEscritorio(), toggleModoEscritorio()
             www/index.html → botón #btnModoEscritorio antes del footer
             www/style.css → reglas body.modo-escritorio

CORRECCIÓN ADICIONAL — DEFORMACIÓN DE FOTOS:
   - Bug: createImageBitmap con resizeWidth Y resizeHeight forzaba imagen cuadrada
   - Solución: eliminado resizeHeight, escala recalculada siempre proporcionalmente
   - Las fotos ahora mantienen su relación de aspecto original (ej: 15x10 → 10x6.66)
   - Archivos: www/app.js → función optimizeImageFileToBlob()

APK GENERADA:
   - Archivo: SET_FOTOGRAFICO_v4.2_debug.apk
   - Reemplaza: SET_FOTOGRAFICO_v3.9_debug.apk
   - Build: 2026-04-19 · Gradle assembleDebug exitoso

=========================================================
SESIÓN CLAUDE – 2026-04-18 (TERCERA ACTUALIZACIÓN)
=========================================================

CORRECCIONES Y MEJORAS IMPLEMENTADAS:

1. CORRECCIÓN ERROR PDF (CUOTA LOCALSTORAGE)
   - Error: "Failed to execute 'setItem' on 'Storage':
     setting the value of 'set_pdf_library_v1' exceeded the quota"
   - Causa: el PDF completo (dataUrl base64) se guardaba en
     localStorage que tiene límite de ~5-10MB
   - Solución: savePdfLibrary() ahora captura el error de cuota y
     reintenta progresivamente:
     · Primero: guarda solo el PDF más nuevo completo, resto sin dataUrl
     · Segundo: guarda solo el más nuevo sin dataUrl
     · Último: omite silenciosamente sin interrumpir la generación
   - La generación del PDF ya NO falla por este error

2. FECHA EN PDF – FORMATO "OSORNO + FECHA" A LA DERECHA
   - Antes: "FECHA: 18 de abril de 2026" centrado en la página
   - Ahora: "OSORNO 18 de Abril del 2026"
   - Posición: alineada a la derecha (desde el margen derecho)
   - Mes siempre con mayúscula inicial, "del" antes del año

3. TEMA OSCURO MOVIDO AL MENÚ DE OPCIONES
   - Eliminado el interruptor (switch) de la pantalla principal
   - Ahora aparece dentro de "MENÚ DE OPCIONES" como botón estilo
     "TEMA OSCURO: OFF / TEMA OSCURO: ON"
   - Mismo diseño que los otros submenús (PLANTILLAS, BORRADORES,
     VER PDF RECIENTES)

4. "PRODUCTIVIDAD" REDUCIDA EN SEGUNDA PANTALLA
   - La palabra PRODUCTIVIDAD era demasiado larga para el recuadro
   - CSS ajustado: font-size reducido a 1.1rem (antes 1.6rem)
   - En dispositivos pequeños (<520px): 0.95rem

5. FUENTE DE PLANTILLA EN "PLANTILLAS GUARDADAS" (NUEVO SET)
   - Al acceder a Nuevo Set > Plantillas Guardadas, el nombre de
     cada plantilla ahora se muestra con la fuente seleccionada
     en esa plantilla (Calibri/Arial → Segoe UI, Times → serif,
     Courier → monospace, etc.)

6. PANTALLA MEMBRETE SIMPLIFICADA
   - Eliminada la opción "IMAGEN" (botón imagen removido)
   - Eliminado el botón "TEXTO" (la pantalla siempre muestra texto)
   - El encabezado "LÍNEAS DE TEXTO (3)" aparece centrado arriba
   - Los 3 campos de texto son inmediatamente visibles sin submenús

7. BOTÓN "VOLVER ATRÁS" EN TODAS LAS PANTALLAS DESDE NUEVO SET
   - Reemplazado "Volver al Menú" por "VOLVER ATRÁS" en: Nuevo Set
     (s9), Título (s2), Fotografías (s3), Datos de Cierre (s4),
     Membrete (s8), Configuración Plantilla (s7)
   - Implementado sistema de historial de navegación (stepHistory)
   - Cada "VOLVER ATRÁS" regresa a la pantalla anterior real
   - Al volver al menú principal (s1) el historial se limpia

8. APK GENERADA
   - Archivo: SET_FOTOGRAFICO_v3.9_debug.apk
   - Reemplaza: SET_FOTOGRAFICO_v3.8_debug.apk
   - Build: 2026-04-18 · Gradle assembleDebug exitoso

=========================================================
SESIÓN CLAUDE – 2026-04-18 (SEGUNDA ACTUALIZACIÓN)
=========================================================

CAMBIOS REALIZADOS:

1. VERSIÓN Y FOOTER
   - Actualizado VERSION_LABEL a "APP BETA CLAUDE 1.0"
   - Build date: 2026-04-17
   - Footer: "CREADA POR HERNAN DIAZ APP BETA CLAUDE 1.0 BUILD 2026-04-17"

2. BOTÓN BORRAR RENOMBRADO
   - Cambio de "Borrar e Iniciar de Nuevo" a "BORRAR E IR A INICIO"

3. COLOR DE NOTA EN ESPAÑOL
   - El selector de color de la nota final ahora muestra en español:
     NEGRO / ROJO / AZUL / AMARILLO (antes mostraba BLACK/RED/BLUE/YELLOW)
   - El textarea de la nota final cambia de color dinámicamente al seleccionar
     (rojo se ve en rojo, azul en azul, etc.)

4. SEGUNDA PANTALLA – IMÁGENES CUADRADAS Y PROPORCIONALES
   - Corregido el problema de la segunda fotografía que se agrandaba y perdía proporción
   - Todas las tarjetas de ilustración tienen ahora ancho fijo (min(88vw, 280px))
   - Las imágenes usan un wrapper de altura fija (90px) con object-fit:cover
   - Las tres ilustraciones lucen uniformes y proporcionales

5. BOTÓN SKIP EN SEGUNDA PANTALLA
   - Agregado botón "SALTAR ›" que aparece de inmediato en la segunda pantalla
   - Posicionado en esquina inferior derecha, sobre el contenido
   - Lleva al menú principal al instante sin esperar la animación completa
   - Estilo translúcido sobre el fondo navy

6. CORRECTOR ORTOGRÁFICO
   - Habilitado spellcheck="true" + lang="es" en los textareas de texto libre:
     · Título del informe (ti_in)
     · Título en revisión (ti_in_review)
     · Descripción de foto (f_txt)
     · Nota final (final_conclusion)
   - El teclado Android y el navegador subrayarán en rojo palabras incorrectas

7. PANELES ACORDEÓN (PRIVACIDAD Y CLIMA)
   - En el menú inicial, los paneles "Privacidad" y "Clima" ahora son acordeones
   - Solo muestran el título con flecha ▼ al inicio
   - Se expanden/colapsan al presionar el panel
   - El clima sigue cargando en background aunque el panel esté colapsado

=========================================================
SESIÓN CLAUDE – 2026-04-17 (PRIMERA ACTUALIZACIÓN)
=========================================================

MEJORAS IMPLEMENTADAS:

1. FIRMA DIGITAL
   - Opción para subir fotografía de firma en "Datos de Cierre" (s4)
   - Opción para subir fotografía de firma en "Nueva Plantilla" (s7)
   - Máximo 5 cm de ancho en el PDF, altura siempre proporcional
   - Si se sube firma digital, reemplaza el pie de firma de texto (nombre + cargo)
   - La firma de la plantilla se guarda y restaura al editar

2. EDITAR PLANTILLA
   - El botón EDITAR ya estaba implementado; se verificó y mejoró la restauración
   - Al editar una plantilla, ahora también restaura la imagen de firma digital

3. NUMERACIÓN CORRELATIVA DE INFORMES
   - Los PDFs se guardan con nombre: DDMMYYYY-NN.pdf (ej: 17042026-01.pdf)
   - El contador es correlativo por día (reinicia cada día)
   - Si se generan 3 PDFs en el mismo día: 17042026-01, -02, -03

4. COMPARTIR PDF
   - En "VER PDF RECIENTES" cada PDF tiene botón "📤 COMPARTIR"
   - Usa Capacitor Share para enviar por WhatsApp, email, etc.
   - En Android, escribe el PDF a carpeta CACHE y abre el diálogo de compartir
   - Fallback: abre en nueva pestaña si Share no está disponible

5. ANCHO DE FOTOS EN PDF: 10 CM
   - Cambiado de 12 cm a 10 cm de ancho fijo
   - La altura siempre es proporcional al aspecto real de la imagen
   - No se deforma en ningún caso

6. DISEÑO PREMIUM NAVY/GOLD
   - Paleta principal: Navy profundo (#1e3a8a) + Oro premium (#c2870d)
   - Header con gradiente navy y sombra
   - Botones con gradientes refinados y sombras por tipo:
     · Principal (azul navy), Acción (oro/ámbar), Secundario (slate),
       Peligro (rojo), Editar (púrpura)
   - Paneles con glassmorphism y sombra sutil
   - Template-items en columna vertical con bordes redondeados
   - h2 con subrayado dorado en lugar de azul

7. ÍCONO PROFESIONAL
   - SVG rediseñado: cámara con lente dorada sobre fondo navy
   - Anillos concéntricos gold/navy para el lente
   - Flash cuadrado dorado, visor pentaprisma
   - Generado en todos los tamaños Android:
     ldpi(36), mdpi(48), hdpi(72), xhdpi(96), xxhdpi(144), xxxhdpi(192)
   - Iconos round e ic_launcher actualizados

=========================================================
HISTORIAL PREVIO (antes de sesiones Claude)
=========================================================

VERSIÓN 3.8 – Estado FREE lista
   - Estado FREE por defecto con enforcement duro y fuente premium validada
   - UI FREE visible (badges en menu/revision) y modal de upsell
   - Límite FREE 6 fotos con bloqueo real
   - Ads solo FREE: banner + 2 videos antes de PDF con aviso previo
   - PDF FREE con watermark más visible y compresión extra
   - Menú Premium con submenú de Plantillas y biblioteca (máx 5)

VERSIÓN 3.0 – Publicidad instalada
   - Capacitor v6 inicializado (appId com.dynamicideas.setfotografico)
   - Plataforma Android creada, web assets movidos a www/
   - AdMob plugin @capacitor-community/admob@6.2.0 instalado
   - AndroidManifest con APPLICATION_ID de AdMob
   - Ads en app: initAds + banner inferior (FREE) + 2 rewarded antes de PDF
   - FREE/PREMIUM: límite 6 fotos, contador en revisión, bloqueo y marca de agua
   - Pantalla Premium, botón premium y slot de publicidad en menú
   - Watermark tipo sello en PDF para versión gratuita

=========================================================
PENDIENTES / IDEAS FUTURAS
=========================================================

- [x] Firma dibujada con el dedo (canvas táctil) — IMPLEMENTADO sesión 2026-04-21
- [ ] Grid de fotos en PDF (2 fotos por fila)
- [ ] Recorte de fotos (crop)
- [ ] Múltiples notas finales con secciones
- [ ] Exportar/importar datos (backup JSON)
- [ ] Vista previa del PDF dentro de la app
- [ ] Drag & drop para reordenar fotos
- [ ] Barra de progreso del informe (1/5, 2/5...)
- [ ] Soporte HEIC (conversión automática desde iPhone)
- [ ] Verificar funcionamiento del corrector ortográfico en dispositivos Android

=========================================================
FIN DE NOTAS
=========================================================
